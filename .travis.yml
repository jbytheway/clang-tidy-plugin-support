dist: xenial
language: c++
os: linux

cache: ccache

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-xenial-8
    packages:
      - clang-8
      - llvm-8-dev

script:
  - ./build.sh
